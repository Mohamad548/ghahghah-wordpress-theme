# ورک‌فلو بهینه‌سازی سرعت — قالب قهقهه

تاریخ: ۲۰۲۶-۰۹-۱۷  
هدف: Core Web Vitals (LCP / CLS / INP) + سرعت رفتن از لینک به لینک

---

## نقشهٔ اندازه‌گیری (قبل از هر فاز)

1. Chrome DevTools → Lighthouse (Mobile + Slow 4G) روی:
   - صفحه اصلی
   - آرشیو محصولات
   - تک‌محصول
   - تماس با ما
2. Performance panel: عنصر LCP را شناسایی کنید (باید hero باشد نه لوگو).
3. Network: تعداد CSS سریالی و حجم تصاویر بالای تأمین اول.
4. بعد از هر فاز P0، دوباره همان ۴ صفحه را اندازه بگیرید و عدد را ثبت کنید.

معیار موفقیت تقریبی (موبایل لاب):

| متریک | هدف |
|--------|------|
| LCP | &lt; 2.5s |
| CLS | &lt; 0.1 |
| INP | &lt; 200ms |
| FCP | &lt; 1.8s |

---

## اندازه‌گیری Lighthouse واقعی (لاب — موبایل simulate)

تاریخ: ۲۰۲۶-۰۹-۱۸  
محیط: `http://localhost:8888` (wp-env) — **نه هاست پروداکشن**  
خروجی خام: `.smoke-evidence/lighthouse-2026-09-18/`

| صفحه | Perf | FCP | LCP | CLS | TBT | وضعیت |
|------|------|-----|-----|-----|-----|--------|
| تماس | **۹۶** | ۰.۹۸s | **۱.۷s** | ۰ | ۵۹ms | عالی |
| FAQ | **۹۸** | ۰.۹۵s | **۱.۶s** | ۰ | ۰ | عالی |
| حریم خصوصی | **۹۷** | ۰.۹۶s | **۱.۶s** | ۰ | ۰ | عالی |
| کارخانه | **۹۸** | ۰.۹۴s | **۲.۱s** | ۰ | ۰ | عالی |
| تک‌محصول | **۹۶** | ۱.۰s | **۱.۹s** | ۰ | ۲ms | عالی |
| عمده | ۸۶ | ۱.۱s | ۳.۹s | ۰ | ۱۱ms | خوب / LCP تصویر پک |
| مقالات | ۸۰ | ۱.۰s | ۴.۲s | ۰ | ۰ | LCP کارت مقاله |
| خانه | ۷۷ | ۱.۴s | ۲.۵s | ۰.۰۱۲ | ۴۴۳ms | نزدیک هدف LCP |
| محصولات | ۷۴ | ۱.۰s | ۴.۵s | ۰.۰۷۹ | ۱۶ms | LCP بنر آپلودشده ~۲۸۰KB |
| نمایندگی | ۷۲ | ۱.۲s | ۳.۸s | ۰ | ۵۴۹ms | LCP پک ۳۵۰KB |

### جمع‌بندی لاب
- **CLS** در همه صفحات زیر ۰.۱ (هدف پاس)
- صفحات متنی/فرم سبک (تماس، FAQ، کارخانه، تک‌محصول) در محدوده عالی
- گلوگاه باقی‌مانده عمدتاً **تصاویر بزرگ آپلود/باندل** در بنر آرشیو و پک‌شات عمده/نمایندگی — نه ساختار قالب
- روی هاست واقعی با کش/CDN معمولاً LCP بهتر از لاب لوکال Docker می‌شود

---

## فاز P0 — همین الان در کد اعمال شد / در حال اعمال

| # | کار | اثر | وضعیت |
|---|-----|-----|--------|
| 1 | تبدیل ذرت آرشیو محصولات/مقالات PNG→WebP (~1.5–1.8MB → ~60–90KB) | LCP آرشیو | انجام |
| 2 | حذف رقابت `fetchpriority=high` لوگو با hero | LCP صفحه اصلی | انجام |
| 3 | `loading=eager` + preload تصویر LCP | LCP | انجام |
| 4 | تماس: ذرت تزئینی بدون high priority / lazy | LCP تماس | انجام |
| 5 | تک‌محصول: `fetchpriority=high` روی تصویر اول | LCP محصول | انجام |
| 6 | توقف تایمر ۳۲ms پیشرفت اسلایدر → CSS animation | INP صفحه اصلی | انجام |
| 7 | CSS زیرfold صفحه اصلی non-blocking | FCP | انجام |
| 8 | Speculation Rules + prefetch مسیرهای اصلی | ناوبری لینک‌به‌لینک | انجام |
| 9 | `no_found_rows` در live-search | INP جستجو | انجام |
| 10 | حذف emoji / block-library / embed از فرانت | TTFB/فایل‌های اضافه | انجام |
| 11 | **ادغام CSS سراسری در `core.css`** (۶ فایل → ۱) | FCP / waterfall | انجام |
| 12 | **Facade نقشه تماس** (کلیک برای لود iframe) | LCP/شبکه صفحه تماس | انجام |

اسکریپت بازسازی CSS: `bash scripts/build-core-css.sh`  
(بعد از ویرایش `fonts/base/layout/header/footer/mobile-bottom-nav.css`)

---

## فاز P1 — باقی‌مانده (هاست + تم)

1. **کش صفحه/آبجکت روی هاست** (LiteSpeed / Redis / Cloudflare) — بزرگ‌ترین برد TTFB.
2. **CDN** برای استاتیک (تصاویر/CSS/JS).
3. **تبدیل فونت‌ها به woff2** و preload همان فایل‌ها.
4. ~~ادغام CSS سراسری~~ ✅
5. ~~نقشه تماس facade~~ ✅
6. **ریدایرکت‌های مهاجرت URL** را در nginx/CDN بگذارید؛ PHP فقط fallback.
7. **کش ابعاد بنرهای hero** تا `getimagesize` تکراری نزند.

---

## فاز P2 — پولیش

1. `filemtime` به‌جای فقط version ثابت برای `?ver=`
2. عرض/ارتفاع برای همه imgهای باقی‌مانده (CLS)
3. یک listener مشترک برای بستن live-search
4. بررسی desktop: CSS نوار پایین فقط در breakpoint موبایل (`media`)

---

## چک‌لیست لینک‌به‌لینک

- [ ] Prefetch/Speculation برای: خانه، محصولات، عمده، نمایندگی، تماس، مقالات
- [ ] منوی هدر و bottom-nav به URLهای نهایی (نه ریدایرکت زنجیره‌ای)
- [ ] بعد از کلیک: بدون JS سنگین در unload؛ ناوبری hard reload معمولی + hint کافی است
- [ ] Live search فقط REST سبک؛ کلیک نتیجه = ناوبری کامل

---

## روی سرور واقعی (اجباری)

1. PHP 8.2+، OPcache روشن
2. Gzip/Brotli
3. کش مرورگر طولانی برای `/wp-content/themes/ghahghah-theme/assets/*`
4. `WP_DEBUG` خاموش
5. تصویرها WebP/AVIF از رسانه وردپرس + سایزهای تولیدشده
6. افزونه کش صفحه (با exclude فرم‌ها/REST در صورت نیاز)

---

## ترتیب اجرا برای تیم

```
اندازه‌گیری پایه
  → P0 تم (این PR)
  → اندازه‌گیری مجدد
  → P1 هاست/CDN/فونت
  → اندازه‌گیری
  → P2 پولیش
  → مانیتورینگ میدانی (CrUX / Search Console)
```
